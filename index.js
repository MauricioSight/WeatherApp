/**
 * @format
 */

import {AppRegistry} from 'react-native';
import WeatherList from './src/screens/WeatherList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => WeatherList);
